// imports

// async functions
async function main() {
     
}

// call main
main()
    .then(() => process.exit(1))
    .catch((error) => {
        console.error(error)
        process.exit(1)
    })